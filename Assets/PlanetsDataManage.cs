using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlanetsDataManage : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    void SavePlanetsToJsonFile(string filepath = "/SaveFiles" + "/byJson.json")
    {

    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
